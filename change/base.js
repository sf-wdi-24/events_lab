console.log("Sanity Check: JS is working!");

$(document).ready(function(){
	$(#total).text(#left + #right) //This doesn't work, I think I might have to add some variables, not sure how to get the numbers to be actual values.
  $("#time").text( Date.now() );

})
